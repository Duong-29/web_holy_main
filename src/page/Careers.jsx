import { div } from "framer-motion/client"

export default function Careers() {
    const styles = {
        mainContainer: `flex flex-col w-full overflow-x-hidden`,
        container1: `flex flex-col w-2/3 items-center `,
    }

    return (
        <div className={styles.mainContainer}>

        </div>
    )
}